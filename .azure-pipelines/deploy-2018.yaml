jobs:
- deployment: Deploy
  displayName: 'Deploy 2018'
  environment:
    name: server
    resourceType: VirtualMachine
  strategy:
    runOnce:
      deploy:
        steps:
        - checkout: self

        - template: steps/docker-compose.yaml
          parameters:
            stage: '2018'