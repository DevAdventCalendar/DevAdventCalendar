@model DevAdventCalendarCompetition.Vms.SendCodeViewModel
@using static DevAdventCalendarCompetition.Resources.ViewsMessages

@{
    ViewBag.Title = @Send;
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("SendCode", "Account", new { ReturnUrl = Model.ReturnUrl }, FormMethod.Post, null, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.Hidden("rememberMe", @Model.RememberMe)
    <h4>@SendCode</h4>
    <hr />
    <div class="row">
        <div class="col-md-8">
            @Choose2Fa
            @Html.DropDownListFor(model => model.SelectedProvider, Model.Providers)
            <input type="submit" value="Zatwierdź" class="btn btn-default" />
        </div>
    </div>
}

@section Scripts {
    Scripts.Render("~/bundles/jqueryval")
}