trigger:
- master

jobs:
- deployment: Deploy
  displayName: 'Deploy Prod'
  environment:
    name: server
    resourceType: VirtualMachine
  strategy:
    runOnce:
      deploy:
        steps:
        - checkout: self

        - template: steps/docker-compose.yaml
          parameters:
            stage: 'prod'