trigger:
- dev_uat

jobs:
- deployment: Deploy
  displayName: 'Deploy UAT'
  environment:
    name: server
    resourceType: VirtualMachine
  strategy:
    runOnce:
      deploy:
        steps:
        - checkout: self

        - template: steps/docker-compose.yaml
          parameters:
            stage: 'uat'